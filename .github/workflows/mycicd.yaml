# name: new-project-cicd

# on: push
  
# jobs:
#   compile:
#     runs-on: ubutnu:latest
#     steps:
#     - name: checkout the code
#       uses: actions/checkout@v4
#     - name: setup jdk
#       uses: actions/setup-java@v5
#       with:
#         java-version: '17'
#         distribution: 'temurin'
#         cache: maven
#     - name: compile the code
#       run: mvn compile
#   security-checks:
#     runs-on: ubutnu-latest
#     steps:
#     - name: checkout
#       uses: actions/checkout@v4
#     - name: trivy installation
#       uses: aquasecurity/setup-trivy@e07451d2e059ed86c2870430ea286b3a9e0bf241
#     - name: Trivy FS Scan
#       run: trivy fs --format table -o fs-report.json .
#     - name: gitleaks installation
#       run: sudo apt install git leaks -y
#     - name: git leaks scan code
#       run: git leaks detect source . -r gitleaks-report.json -f json
#   test-code:
#     runs-on: ubutnu-latest
#     steps:
#     - name: checkout
#       uses: actions/checkout@v4
#     - name: setup jdk
#       uses: actions/setup-java@v5
#       with:
#         java-version: '17'
#         distribution: 'temurin'
#         cache: maven
#     - name: unit test cases
#       run: mvn test
#   sonar-build-and-scan:
#     runs-on: ubuntu-latest
#     steps:
#     - name: checkout
#       uses: actions/checkout@v4
#     - name: setup jdk
#       uses: actions/setup-java@v5
#       with:
#         java-version: '17'
#         distribution: 'temurin'
#         cache: maven
#     - name: build the package
#       run: mvn package
#     - name: upload jar artifact 
#       uses: actions/upload-artifact@v4
#       with:
#         name: my-artifact
#         path: target/*.jar
#     - uses: actions/checkout@v4
#       with:
#         # Disabling shallow clones is recommended for improving the relevancy of reporting
#         fetch-depth: 0
#     - name: SonarQube Scan
#       uses: SonarSource/sonarqube-scan-action@5.0.0 # Ex: v4.1.0 or sha1, See the latest version at https://github.com/marketplace/actions/official-sonarqube-scan
#       env:
#         SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}       
#         SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
#     - name: SonarQube Quality Gate check
#       id: sonarqube-quality-gate-check
#       uses: sonarsource/sonarqube-quality-gate-action@master
#       with:
#         pollingTimeoutSec: 600 
#       env:
#        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
#        SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
#   build-image-and-push:
#     runs-on: ubuntu-latest
#     steps: 
#     - name: configure aws creds
#       uses: aws-actions/configure-aws-credentials@v6.0.0
#       with:
#         aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#         aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#         aws-region: us-east-2
#     - name: login ecr
#       id: login-ecr
#       uses: aws-actions/amazon-ecr-login@v2         
#     - name: Set up Docker Buildx
#       uses: docker/setup-buildx-action@v3
#     - name: Build and push
#       uses: docker/build-push-action@v6
#       with:
#         context: .
#         push: true
#         tags: |
#           ${{ steps.login-ecr.outputs.registry }}/${{ secrets.ECR_REPO }}:latest
#           ${{ steps.login-ecr.outputs.registry }}/${{ secrets.ECR_REPO }}:${{ github.sha }}